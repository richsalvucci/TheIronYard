console.log("index")
$("ul.patients").html("");
<% @patients.each do |patient| %>
  $("ul.patients").append("<%= escape_javascript(render(partial: 'hospitals/patient', locals: { patient: patient})) %>");
<% end %>